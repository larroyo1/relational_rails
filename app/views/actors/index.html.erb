<h1>Actors</h1>

<ol>
<% @actors.order('created_at DESC').each do |actor| %>
<li><%= link_to "#{actor.name}", "/actors/#{actor.id}" %><%= " was created_at #{actor.created_at}." %></li>

<% end  %>
</ol>

<%= link_to 'Create new Actor', actors_new_path %>
